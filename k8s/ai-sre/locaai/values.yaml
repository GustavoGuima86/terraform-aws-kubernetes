# values.yaml for local-ai
persistence:
  pvc:
    enabled: true
    size: 10Gi
    accessModes:
      - ReadWriteOnce

models:
  forceDownload: false
  list:
    - url: "https://gpt4all.io/models/ggml-gpt4all-j.bin"

deployment:
  env:
    modelsPath: "/models"
