apiVersion: v1
kind: ConfigMap
metadata:
  name: holmes-model-list
  labels:
    app.kubernetes.io/name: holmes
    app.kubernetes.io/component: model-list
  # Optionally set namespace if needed
  # namespace: {{ .Release.Namespace }}
data:
  model_list.yaml: |
    {{- toYaml .Values.config.model_list | nindent 4 }}

