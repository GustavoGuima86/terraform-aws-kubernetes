apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: observability
  source:
    repoURL: https://github.com/GustavoGuima86/terraform-aws-kubernetes
    targetRevision: new-2026-features
    path: k8s/observability/loki
    helm:
      valueFiles:
        - values.yaml
      parameters:
      - name: loki.config.common.storage.s3.bucketnames
        value: "{{ .Values.lokiBucketName }}"
      - name: loki.config.common.storage.s3.region
        value: "{{ .Values.awsRegion }}"
      - name: loki.clusterName
        value: "{{ .Values.eksClusterName }}"
      - name: loki.awsRegion
        value: "{{ .Values.awsRegion }}"
  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
