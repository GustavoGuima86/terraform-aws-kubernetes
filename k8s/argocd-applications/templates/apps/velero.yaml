apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: velero
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ .Values.gitRepo.url }}
    targetRevision: {{ .Values.gitRepo.branch }}
    path: k8s/velero
    helm:
      valueFiles:
        - values.yaml
      parameters:
        - name: configuration.backupStorageLocation.bucket
          value: "{{ .Values.velero_bucket_name }}"
        - name: configuration.backupStorageLocation.config.region
          value: "{{ .Values.velero_bucket_region }}"
  destination:
    server: https://kubernetes.default.svc
    namespace: velero
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

